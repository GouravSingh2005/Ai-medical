# ============================================
# Agentic AI Medical System - Environment Configuration
# ============================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env file to Git!

# ============================================
# DATABASE CONFIGURATION (Required)
# ============================================
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password_here
DB_NAME=medical_ai_system

# ============================================
# OPENAI CONFIGURATION (Required)
# ============================================
# Get API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini

# ============================================
# GOOGLE MAPS CONFIGURATION (Required)
# ============================================
# Get API key from: https://console.cloud.google.com/
# Enable: Distance Matrix API & Maps JavaScript API
GOOGLE_MAPS_API_KEY=AIzaSy-your-google-maps-api-key-here

# ============================================
# EMAIL CONFIGURATION (Optional - Gmail Example)
# ============================================
# For Gmail: Create App Password at https://myaccount.google.com/security
# Enable 2FA first, then go to App Passwords section
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=xxxx xxxx xxxx xxxx

# Alternative SMTP Providers:
# SendGrid: SMTP_HOST=smtp.sendgrid.net, SMTP_PORT=587
# Mailgun: SMTP_HOST=smtp.mailgun.org, SMTP_PORT=587
# AWS SES: SMTP_HOST=email-smtp.region.amazonaws.com, SMTP_PORT=587

# ============================================
# WHATSAPP CONFIGURATION (Optional - Twilio)
# ============================================
# Get credentials from: https://www.twilio.com/console
# For testing: Use Twilio WhatsApp Sandbox
# For production: Apply for WhatsApp Business API
TWILIO_ACCOUNT_SID=your_twilio_account_sid_here
TWILIO_AUTH_TOKEN=your_twilio_auth_token_here
TWILIO_WHATSAPP_FROM=+1234567890

# WhatsApp Sandbox Setup:
# 1. Go to Twilio Console → Messaging → Try it out → Send a WhatsApp message
# 2. Send "join <your-code>" to +1 415 523 8886 from your WhatsApp
# 3. Use sandbox number above for TWILIO_WHATSAPP_FROM

# ============================================
# SERVER CONFIGURATION
# ============================================
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# ============================================
# NOTES
# ============================================
# Minimum Required:
#   - Database credentials (DB_*)
#   - OpenAI API key
#   - Google Maps API key
#
# Optional Services (System works without these, but features disabled):
#   - SMTP (Email reports will be skipped)
#   - Twilio (WhatsApp messages will be skipped)
#
# For detailed setup instructions, see:
#   Backend/ENV_CONFIGURATION.md
